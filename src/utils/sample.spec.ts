// @ai-generated
// Implements: TASK-001 — Initialize Vite + Vue 3 + TypeScript project
// Spec ref:   SPEC.md §1
import { describe, it, expect } from 'vitest'

// Smoke test to verify Vitest is configured correctly
describe('vitest setup', () => {
  it('runs a basic test', () => {
    expect(1 + 1).toBe(2)
  })
})
